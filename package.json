{
  "name": "playground",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "setup:catalog": "node scripts/setup-catalog-table.js",
    "seed:genres": "node scripts/seed-genres.js",
    "seed:artists": "node scripts/seed-artists.js",
    "seed:all": "npm run seed:genres && npm run seed:artists",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.587.0",
    "@aws-sdk/lib-dynamodb": "^3.587.0",
    "dotenv": "^16.4.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
